<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- 半透明背景設定 -->
    <drawable name="transparameter">#9f000000</drawable>
    <style name="Theme.TranslucentBackground" parent="AppTheme">
        <item name="android:windowBackground">@drawable/transparameter</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <!-- 半透明背景設定 -->
    <drawable name="transparameterClear">#01000000</drawable>
    <style name="Theme.TranslucentBackgroundClear" parent="AppTheme">
        <item name="android:windowBackground">@drawable/transparameterClear</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="TextAppearance.Toolbar" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:fontFamily">@font/nicokaku_v1</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="custom_button" parent="@style/Widget.AppCompat.Button.Borderless.Colored">
        <item name="fontFamily">@font/nicokaku_v1</item>
        <item name="android:textColor">#FFF</item>
    </style>

    <style name="back_button" parent="@style/Widget.AppCompat.Button.Borderless.Colored">
        <item name="fontFamily">@font/nicokaku_v1</item>
        <item name="android:textColor">@android:color/holo_blue_dark</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingLeft">-5dp</item>
    </style>
</resources>
